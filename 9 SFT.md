第9章：指令微调 (SFT)（构建高质量 Instruction Dataset 与数据格式化技巧）

在大语言模型（LLM）的技术栈中，指令微调（Supervised Fine-Tuning, SFT）是连接“基础预训练模型”与“可直接落地的实用模型”的关键桥梁。预训练模型如同掌握了海量语言知识的“学者”，而SFT则是通过“指令训练”，教会这位“学者”听懂人类指令、精准完成任务——无论是问答、总结、翻译，还是代码生成、逻辑推理，SFT都能让模型从“被动理解语言”转向“主动执行指令”。

SFT的核心价值的在于“对齐人类意图”：预训练模型学习的是文本的统计规律和通用知识，而SFT通过人工或半自动构建的“指令-响应”数据集，让模型学习“什么样的指令对应什么样的输出”，最终实现“指令输入→精准响应”的闭环。而SFT的效果，80%取决于数据集的质量——高质量的指令数据集能让模型快速掌握任务逻辑、贴合人类需求，反之，低质量数据不仅会浪费算力，还可能导致模型出现指令混淆、输出偏差等问题。

本章将聚焦SFT的核心环节：先拆解高质量Instruction Dataset的构建逻辑、数据来源、筛选标准与清洗方法，再详细讲解数据格式化的核心技巧、主流格式规范，最后结合实操案例，帮助读者快速落地SFT数据集的构建与格式化，为后续模型微调奠定基础。

# 9.1 指令微调（SFT）核心认知：为什么数据集是关键？

在深入数据集构建之前，我们需要先明确SFT的核心逻辑的，以及为什么“数据质量”比“数据量”更重要。

## 9.1.1 SFT的本质：让模型“听懂指令、学会响应”

预训练模型（如LLaMA、GPT-3 Base）的训练目标是“预测下一个token”，其输出是“概率化的文本续写”，无法主动响应人类的具体指令——比如给预训练模型输入“总结下面这段话”，它可能只会续写文本，而不会执行“总结”任务。

SFT的本质，是通过“有监督学习”，让模型学习“指令（Instruction）→输入（Input，可选）→响应（Response）”的映射关系。简单来说，就是给模型大量“指令-响应”样本，让它学会：当收到某种类型的指令时，应该输出什么样的内容、遵循什么样的逻辑。

例如：

- 指令：“翻译下面的英文句子为中文”，输入：“Hello, world!”，响应：“你好，世界！”
- 指令：“总结这段文字的核心观点”，输入：“大语言模型的发展依赖预训练、SFT、RLHF三大环节，其中SFT是对齐人类意图的关键”，响应：“大语言模型发展需三大环节，SFT是对齐人类意图的核心”
- 指令：“编写一个Python函数，计算两个数的和”，输入：“无”，响应：“def add(a, b):\n    return a + b”

这些样本构成了SFT的核心数据集，模型通过学习这些样本，逐渐形成“指令理解→任务执行→响应生成”的能力，最终实现“输入指令，就能输出符合预期的结果”。

## 9.1.2 数据集对SFT的影响：质量决定上限

SFT的效果，核心取决于两个因素：数据集质量和微调参数。其中，数据集的质量直接决定了模型的“指令跟随能力”上限——即使使用最优的微调参数，若数据集存在指令模糊、响应错误、逻辑混乱等问题，模型也无法学会正确的指令响应逻辑。

举个反例：若数据集中存在“指令是翻译，响应却是总结”“指令要求简洁，响应却冗长”“响应存在事实错误”等问题，模型会学习到错误的映射关系，最终出现“指令不匹配”“输出错误”等问题，甚至会退化预训练模型的原有能力。

因此，构建SFT数据集的核心原则是：**少而精，而非多而杂**。对于中小规模模型（如7B、13B），高质量的10k-100k样本，远比低质量的1M样本效果更好。

# 9.2 构建高质量Instruction Dataset：全流程指南

高质量的Instruction Dataset，需要满足“指令多样、输入合理、响应精准、逻辑一致”四大核心要求。其构建流程可分为“明确任务范围→数据来源选择→数据筛选→数据清洗→数据增强”五个环节，每个环节都有明确的标准和实操技巧。

## 9.2.1 第一步：明确任务范围——避免数据集“泛而不精”

构建数据集前，首先要明确SFT的目标：模型需要具备哪些指令响应能力？是通用指令（如问答、总结、翻译），还是特定领域指令（如法律咨询、代码生成、医疗问答）？不同的目标，决定了数据集的任务类型和覆盖范围。

核心原则：**聚焦核心任务，避免“大而全”**。如果模型的目标是“通用对话”，则覆盖问答、总结、翻译、创意写作等通用任务；如果目标是“法律领域助手”，则重点围绕法律咨询、法条解读、案例分析等任务构建数据，避免混入无关领域（如医疗、代码）的样本。

任务范围的明确，能避免两个问题：一是数据集冗余，增加微调算力成本；二是模型注意力分散，无法精准掌握核心任务的响应逻辑。

实操建议：列出3-5个核心任务（如“通用问答、文本总结、多语言翻译”），每个任务明确“指令类型、输入格式、响应标准”，作为后续数据收集和筛选的依据。

## 9.2.2 第二步：数据来源选择——兼顾质量与效率

SFT数据集的来源主要分为三大类：人工标注、公开数据集复用、半自动生成。不同来源各有优劣，实际应用中可结合使用，兼顾质量和效率。

### 9.2.2.1 人工标注：质量最高，成本最高

人工标注是构建高质量数据集的核心方式，由标注人员根据任务要求，手动编写“指令-输入-响应”样本。其优势是“指令清晰、响应精准、逻辑一致”，能完美匹配模型的微调目标；劣势是标注成本高、周期长，适合核心任务的样本构建。

人工标注的关键要求：

- 标注人员需熟悉任务场景（如法律领域标注需具备基础法律知识）；
- 制定明确的标注规范（如“指令需简洁明了，响应需准确、完整，避免冗余”）；
- 进行标注审核，剔除标注错误、逻辑混乱的样本。

适用场景：核心任务的关键样本、需要高度贴合特定需求的样本（如企业内部的专属指令）。

### 9.2.2.2 公开数据集复用：效率高，需二次筛选

目前有大量公开的SFT数据集，可直接复用或二次加工，节省标注成本。主流公开数据集包括：

- 通用指令数据集：Alpaca、ShareGPT、UltraChat、MOSS-003-SFT等，覆盖问答、总结、翻译、创意写作等通用任务；
- 特定领域数据集：LegalBench（法律）、MedQA（医疗）、CodeAlpaca（代码）、MathQA（数学）等，聚焦特定领域的指令任务；
- 顶会数据集：如GLUE、SuperGLUE的变体，可用于指令化改造（将原任务转化为“指令-响应”格式）。

复用公开数据集的关键：**二次筛选和清洗**。公开数据集可能存在“指令模糊、响应错误、样本重复、任务不匹配”等问题，需筛选出与自身任务匹配的样本，剔除低质量内容。

实操建议：优先选择知名度高、标注质量有保障的数据集（如Alpaca、ShareGPT），避免使用来源不明、未经过审核的数据集。

### 9.2.2.3 半自动生成：平衡质量与效率

对于需要大量样本的场景，可采用“半自动生成”的方式：利用预训练模型或现有高质量样本，自动生成“指令-响应”样本，再进行人工审核和修正。这种方式既能节省标注成本，又能保证样本质量。

常见的半自动生成方法：

- 基于现有样本改写：将已有的“指令-响应”样本进行同义改写（如修改指令的表述、调整输入的措辞、优化响应的句式），生成新的样本；
- 模型生成+人工审核：用高质量的预训练模型（如GPT-4、Claude），输入“任务类型+指令模板”，让模型生成响应，再由人工审核修正错误、优化逻辑；
- 数据蒸馏：将大模型（如GPT-4）的响应作为“伪标签”，生成大量样本，用于中小模型的SFT（需注意：大模型的响应可能存在错误，需人工抽样审核）。

注意：半自动生成的样本，必须经过人工审核，剔除错误、冗余、逻辑混乱的内容，否则会影响模型微调效果。

## 9.2.3 第三步：数据筛选——筛选“优质样本”，剔除“噪声样本”

无论采用哪种数据来源，都需要进行数据筛选，筛选出符合“高质量标准”的样本，剔除噪声样本。筛选的核心标准的围绕“指令、输入、响应”三个维度展开。

### 9.2.3.1 指令筛选标准

- 清晰明确：指令需准确表达任务需求，避免模糊不清（如避免“处理这段文字”，应明确“总结这段文字”“翻译这段文字”）；
- 多样性：覆盖不同的指令表述（如“总结”可表述为“概括核心观点”“提炼重点”“简要总结”），避免指令句式单一，让模型学会理解不同表述的同一指令；
- 合理性：指令需符合模型的能力范围，避免超出模型认知（如避免让通用模型执行“复杂的量子力学计算”）；
- 无歧义：避免一个指令对应多个任务（如避免“总结并翻译这段文字”，可拆分为两个独立指令）。

### 9.2.3.2 输入筛选标准

- 相关性：输入需与指令高度相关（如指令是“翻译英文”，输入必须是英文文本，避免混入中文文本）；
- 完整性：输入需完整，避免缺失关键信息（如指令是“解答数学题”，输入需包含完整的题目条件，避免只给部分条件）；
- 合理性：输入长度适中（避免过短或过长，一般建议输入长度在10-500token之间，根据模型上下文长度调整），避免无意义的输入（如乱码、重复文本）。

### 9.2.3.3 响应筛选标准

- 准确性：响应需符合指令要求，无事实错误（如翻译需准确，问答需正确，代码需可运行）；
- 完整性：响应需完整覆盖指令需求（如指令是“总结3个核心观点”，响应需明确列出3个观点，避免遗漏）；
- 简洁性：响应需简洁明了，避免冗余（如总结无需重复原文，翻译无需添加额外解释，除非指令明确要求）；
- 逻辑性：响应需逻辑清晰，条理分明（如复杂任务的响应可分点说明，代码需格式规范）；
- 一致性：相同或相似的指令+输入，响应的风格、逻辑需一致（如同样是“总结”，避免有的响应冗长、有的响应简洁）。

### 9.2.3.4 筛选实操技巧

1. 先进行“粗筛”：剔除明显不符合标准的样本（如指令模糊、响应错误、输入无关）；
2. 再进行“精筛”：对粗筛后的样本，逐一审核指令、输入、响应的质量，修正轻微问题（如指令表述不规范、响应冗余），剔除无法修正的低质量样本；
3. 控制样本数量：根据模型规模调整样本数量（7B模型建议10k-50k样本，13B模型建议50k-100k样本，34B及以上模型可适当增加），避免样本过多导致过拟合，样本过少导致模型学不会指令逻辑。

## 9.2.4 第四步：数据清洗——修正问题样本，保证数据一致性

筛选后的样本，可能存在一些细节问题（如格式混乱、错别字、标点错误、表述不规范），需要进行数据清洗，确保数据集的一致性和规范性，减少模型学习的干扰。

核心清洗内容：

### 9.2.4.1 格式清洗

统一样本的格式（如指令、输入、响应的分隔符，换行格式，标点符号），避免格式混乱。例如：统一用“### 指令：”“### 输入：”“### 响应：”作为分隔符，避免有的样本用“指令：”，有的样本用“Question：”。

### 9.2.4.2 文本清洗

- 修正错别字、语病、标点错误（如“的/地/得”混用、逗号缺失、错别字）；
- 去除冗余内容（如响应中重复的句子、无关的解释、乱码、特殊符号）；
- 统一表述风格（如通用指令的响应采用正式、简洁的风格，避免口语化；创意写作的响应可灵活调整，但需保持一致性）；
- 规范专业术语（如特定领域的术语，需统一表述，避免同一术语有多种写法）。

### 9.2.4.3 去重处理

剔除重复样本或高度相似的样本（如完全相同的“指令-输入-响应”，或指令、输入相同，响应仅细微差异的样本），避免模型过度学习同一内容，导致泛化能力下降。

实操技巧：可使用文本相似度算法（如余弦相似度），筛选出相似度高于90%的样本，进行去重处理。

### 9.2.4.4 逻辑修正

对于筛选后仍存在轻微逻辑问题的样本（如响应逻辑不连贯、指令与响应轻微不匹配），进行人工修正，确保“指令-输入-响应”的逻辑闭环。

## 9.2.5 第五步：数据增强——提升数据集多样性，增强模型泛化能力

数据增强的目的是通过“合理改造现有样本”，增加数据集的多样性，让模型学习到更多不同场景下的指令响应逻辑，提升泛化能力。需要注意的是，数据增强不能改变样本的核心逻辑和质量，避免引入新的噪声。

常用的数据增强方法：

### 9.2.5.1 同义改写增强

对指令、输入、响应进行同义改写，生成新的样本，保持核心含义不变，仅改变表述方式。例如：

- 原指令：“总结这段文字的核心观点”；改写后：“提炼这段文字的重点内容”“概括这段文字的核心思想”；
- 原输入：“大语言模型的SFT是对齐人类意图的关键”；改写后：“对齐人类意图的核心环节是大语言模型的SFT”；
- 原响应：“SFT能让模型学会响应人类指令”；改写后：“SFT可使模型掌握人类指令的响应方法”。

### 9.2.5.2 输入变体增强

针对同一指令，生成不同的输入样本，让模型学会处理不同输入场景。例如：

- 指令：“翻译英文句子为中文”；
- 输入1：“I love programming.”；响应：“我喜欢编程。”；
- 输入2：“Programming is my hobby.”；响应：“编程是我的爱好。”。

### 9.2.5.3 任务扩展增强

在核心任务的基础上，扩展任务场景，增加样本的多样性。例如：核心任务是“文本总结”，可扩展为“总结新闻文本”“总结学术论文摘要”“总结对话内容”等不同场景的样本。

### 9.2.5.4 长度变体增强

针对同一指令，生成不同长度的输入和响应，让模型适应不同长度的任务。例如：指令“总结文本”，可生成“短文本总结”“长文本总结”两种样本，响应长度分别对应简短概括和详细总结。

注意：数据增强的比例不宜过高，建议增强后的样本数量不超过原始样本的50%，避免过度增强导致样本质量下降。

# 9.3 数据格式化技巧：让模型“读懂”数据集

SFT数据集的格式化，是连接“数据集”与“模型微调”的关键——即使是高质量的样本，若格式不规范，模型也无法正确解析“指令-输入-响应”的映射关系，导致微调失败。

格式化的核心原则：**统一、清晰、贴合模型输入习惯**。不同的模型（如LLaMA、ChatGLM、Qwen）对输入格式的要求略有差异，但整体遵循“指令+输入+响应”的结构化格式，关键是让模型能明确区分“指令是什么、输入是什么、需要输出什么”。

## 9.3.1 通用格式化规范（适配绝大多数LLM）

通用的SFT数据格式采用“分隔符+结构化文本”的方式，明确区分指令、输入、响应三个部分，常用的分隔符包括“###”“---”“<指令>”等，推荐使用“###”作为分隔符（简洁、清晰，适配大多数模型）。

### 9.3.1.1 基础格式（无输入场景）

当指令不需要额外输入（如“写一首关于春天的诗”“解释什么是SFT”）时，格式为：

```text
### 指令：{指令内容}
### 响应：{响应内容}
```

示例：

```text
### 指令：写一首关于春天的短诗
### 响应：春风拂过柳丝柔，
细雨润开桃瓣羞。
燕语呢喃寻旧垒，
人间三月尽温柔。
```

### 9.3.1.2 标准格式（有输入场景）

当指令需要额外输入（如“总结这段文字”“翻译这段英文”）时，格式为：

```text
### 指令：{指令内容}
### 输入：{输入内容}
### 响应：{响应内容}
```

示例：

```text
### 指令：总结这段文字的核心观点
### 输入：指令微调（SFT）是大语言模型对齐人类意图的关键环节，其核心是通过“指令-响应”样本训练模型，让模型学会听懂指令、精准执行任务。SFT的效果主要取决于数据集的质量，高质量的数据集能让模型快速掌握任务逻辑。
### 响应：SFT是大语言模型对齐人类意图的关键，通过“指令-响应”样本训练模型，其效果取决于数据集质量。
```

### 9.3.1.3 复杂任务格式（多轮指令/多输入场景）

对于多轮指令（如多轮对话、复杂任务拆解）或多输入场景，可采用“多轮分隔”的格式，明确每一轮的指令、输入和响应：

```text
### 第一轮
指令：{第一轮指令}
输入：{第一轮输入}
响应：{第一轮响应}

### 第二轮
指令：{第二轮指令}
输入：{第二轮输入}
响应：{第二轮响应}
```

示例（多轮对话场景）：

```text
### 第一轮
指令：介绍什么是SFT
输入：无
响应：指令微调（SFT）是大语言模型的一种有监督微调方式，通过“指令-响应”样本训练模型，让模型学会听懂人类指令、精准执行任务，是对齐人类意图的关键环节。

### 第二轮
指令：SFT和预训练的区别是什么
输入：无
响应：预训练的目标是让模型学习海量文本的统计规律和通用知识，输出是文本续写；而SFT的目标是让模型学习“指令-响应”的映射关系，输出是符合指令要求的响应，核心是对齐人类意图。
```

## 9.3.2 不同模型的格式化适配技巧

不同的大语言模型，由于预训练方式和结构不同，对输入格式的适配性略有差异，需根据模型特点调整格式，提升微调效果。

### 9.3.2.1 LLaMA/LLaMA 2 系列

LLaMA系列模型对简洁的格式更敏感，推荐使用“### 指令：”“### 响应：”的基础格式，无需额外添加多余的标签，避免格式复杂导致模型解析错误。

适配示例：

```text
### 指令：翻译下面的英文句子为中文
### 输入：The development of large language models has brought new opportunities to artificial intelligence.
### 响应：大语言模型的发展为人工智能带来了新的机遇。
```

### 9.3.2.2 ChatGLM 系列

ChatGLM系列模型采用“对话式格式”，推荐使用“用户：”“助手：”作为分隔符，更贴合模型的预训练习惯，微调效果更好。

适配示例：

```text
用户：翻译下面的英文句子为中文：The development of large language models has brought new opportunities to artificial intelligence.
助手：大语言模型的发展为人工智能带来了新的机遇。
```

### 9.3.2.3 Qwen 系列

Qwen系列模型支持多种格式，推荐使用“<|im_start|>user<|im_end|>”“<|im_start|>assistant<|im_end|>”的标签格式，明确区分用户指令和模型响应，适配模型的对话机制。

适配示例：

```text
<|im_start|>user
翻译下面的英文句子为中文：The development of large language models has brought new opportunities to artificial intelligence.
<|im_end|>
<|im_start|>assistant
大语言模型的发展为人工智能带来了新的机遇。
<|im_end|>
```

## 9.3.3 格式化避坑指南

在数据格式化过程中，容易出现一些细节问题，导致模型无法正确解析样本，需重点规避：

### 9.3.3.1 避免分隔符混乱

统一整个数据集的分隔符，避免有的样本用“###”，有的用“---”，有的用“<指令>”，否则模型无法区分指令、输入、响应的边界。

### 9.3.3.2 避免多余空格和换行

样本中避免出现多余的空格、空行（除了分隔不同样本或多轮对话），否则会增加模型的解析难度，甚至导致模型学习到“多余空格”的错误习惯。

### 9.3.3.3 避免指令/响应缺失

每个样本必须包含“指令”和“响应”，有输入的场景必须包含“输入”，避免出现“只有指令没有响应”“只有输入没有指令”的样本，否则模型无法学习到完整的映射关系。

### 9.3.3.4 控制文本长度

单个样本的总长度（指令+输入+响应）不宜超过模型的上下文长度（如LLaMA 7B的上下文长度为2048token），避免模型无法完整解析样本，导致微调失败。

### 9.3.3.5 统一编码格式

数据集的编码格式统一为UTF-8，避免出现乱码（如中文乱码、特殊符号乱码），否则会影响模型的学习效果。

# 9.4 实操案例：构建一个高质量SFT数据集（以通用问答任务为例）

结合本章所学内容，我们以“通用问答任务”为例，完整演示高质量SFT数据集的构建与格式化流程，帮助读者快速落地实操。

## 9.4.1 明确任务范围

核心任务：通用问答（覆盖日常常识、科学知识、生活技巧等领域）；

任务要求：指令清晰（明确“提问”），输入为“问题”，响应为“准确、简洁的答案”；

样本数量：计划构建10k高质量样本（人工标注3k+公开数据集复用5k+半自动生成2k）。

## 9.4.2 数据来源选择

- 人工标注：组织3名标注人员，围绕日常常识、科学知识等领域，手动编写“问题（输入）+答案（响应）”，指令统一为“回答下面的问题”；
- 公开数据集复用：复用Alpaca、ShareGPT中的通用问答样本，筛选出符合要求的5k样本；
- 半自动生成：用GPT-4生成2k通用问答样本（输入“生成100个日常常识问题及答案”，生成后人工审核修正）。

## 9.4.3 数据筛选与清洗

1. 粗筛：剔除“问题模糊”“答案错误”“无关领域”的样本（如剔除涉及专业领域过深的问题、答案存在事实错误的样本）；
2. 精筛：逐一审核样本，确保“指令清晰、问题完整、答案准确简洁”，修正轻微问题（如错别字、标点错误）；
3. 去重：用余弦相似度算法，剔除相似度高于90%的重复样本；
4. 清洗：统一格式、修正语病、去除冗余内容，确保样本一致性。

## 9.4.4 数据格式化

适配LLaMA 7B模型，采用通用标准格式，示例如下：

```text
### 指令：回答下面的问题
### 输入：什么是人工智能？
### 响应：人工智能（AI）是研究、开发用于模拟、延伸和扩展人的智能的理论、方法、技术及应用系统的一门新技术科学。

### 指令：回答下面的问题
### 输入：如何快速缓解轻微头痛？
### 响应：可通过以下方式缓解轻微头痛：1. 休息片刻，避免强光和噪音刺激；2. 适量饮用温水；3. 轻轻按摩太阳穴；4. 避免过度劳累和精神紧张。
```

## 9.4.5 数据集验证

数据集构建完成后，抽取10%的样本（1k个）进行人工验证，检查样本质量、格式规范性，确保：

- 样本无错误、无冗余、无重复；
- 格式统一、清晰，模型可正确解析；
- 指令、输入、响应的逻辑一致。

验证通过后，即可用于SFT模型微调。

# 9.5 本章总结

指令微调（SFT）的核心是“对齐人类意图”，而高质量的Instruction Dataset是SFT成功的关键。本章从SFT的核心认知出发，详细讲解了高质量数据集的构建流程——从任务范围明确、数据来源选择，到数据筛选、清洗、增强，每一步都围绕“质量优先”的原则，确保样本“指令清晰、输入合理、响应精准、逻辑一致”。

数据格式化是SFT数据集落地的重要环节，需遵循“统一、清晰、贴合模型输入习惯”的原则，根据不同模型的特点调整格式，避免细节问题导致微调失败。通过本章的学习，读者可掌握SFT数据集的构建与格式化技巧，结合实操案例，快速落地高质量SFT数据集的制作，为后续模型微调、RLHF等环节奠定坚实基础。

下一章，我们将聚焦SFT的模型微调实操，讲解微调参数设置、训练流程、效果评估等核心内容，帮助读者将构建好的数据集转化为可落地的实用模型。